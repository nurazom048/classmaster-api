// enums.ts
export enum ActiveStatus {
    NOT_JOINED = "not_joined",
    JOINED = "joined",
    PENDING = "pending",
}

//
export enum imageStorageProviderTS {
    FIREBASE = "firebase",
    AWS = "aws",
    OTHHERS = "others",
    MINIO = "minio",

}
